apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-idiot-chart.fullname" . }}
  labels:
    {{- include "my-idiot-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-idiot-chart.selectorLabels" . | nindent 4 }}
